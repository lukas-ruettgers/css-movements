# css-movements

This repository contains a custom multi-agent grid simulation environment implemented using **PettingZoo** and **NumPy**.
The simulation environment allows to study the dynamics of armed conflicts in civil wars. 

---

## 📦 Installation

It is recommended to create a **Python virtual environment** before installing the dependencies.

### 1. Create and activate a virtual environment

```bash
# Create a virtual environment named .venv
python3 -m venv .venv

# Activate the environment (Linux/macOS)
source .venv/bin/activate

# Activate the environment (Windows)
.\.venv\Scripts\activate
